server {
    listen 80;
    server_name zetalent-media.com www.zetalent-media.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name zetalent-media.com www.zetalent-media.com;

    ssl_certificate     /etc/letsencrypt/live/zetalent-media.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zetalent-media.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    root /var/www/zetalent/frontend_dist;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;

    location /api {
        proxy_pass         http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location ^~ /uploads/ {
        proxy_pass         http://localhost:4000/uploads/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location /assets/images/uploads/ {
        alias /var/www/zetalent/old_uploads/;
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }

    # Social crawler OG meta injection for news articles
    location ~ ^/news/(.+)$ {
        set $is_crawler 0;
        if ($http_user_agent ~* "(whatsapp|facebookexternalhit|twitterbot|linkedinbot|telegrambot|slackbot|discordbot|googlebot|bingbot|applebot)") {
            set $is_crawler 1;
        }
        if ($is_crawler = 1) {
            proxy_pass http://localhost:4000/og/news/$1;
        }
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
