{
  "name": "zetalent",
  "version": "1.0.0",
  "description": "ZeTalent Media Platform",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "node backend/src/index.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
